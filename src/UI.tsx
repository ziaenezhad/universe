import { Component, ReactNode } from 'react';
import './styles/UI.scss';

export default class UI extends Component {
  render(): ReactNode {
    return (
      <section>
        <header>
          بسم الله
        </header>
        <main></main>
        <footer></footer>
      </section>
    );
  }
}
